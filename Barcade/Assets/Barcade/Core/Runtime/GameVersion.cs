namespace Barcade.Core
{
    /// <summary>
    /// Holds the current game version string for the barcade project.
    /// Pure C# — no MonoBehaviour dependency.
    /// </summary>
    public static class GameVersion
    {
        public const string Value = "0.1.0";
    }
}
