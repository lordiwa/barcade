{
  "key": "TASK-056",
  "title": "Dead-seat detection (detección de puesto muerto): idle seat → Idle status, excluded from round payout",
  "description": "Filed from TASK-054's AC3 audit (dev-t101, item 5). GDD Annex D.1 'Adaptador HID → InputSnapshot' row / §3.2 names 'detección de puesto muerto': a seat with no input for ~45 s is marked Idle — excluded from that round's payout but NOT eliminated from the session (they can resume by touching the controls). FINDING: this is entirely unimplemented — no per-seat last-input-edge tracking or Idle marking exists in ArcadeInputBridge, InputInterpreter, or SessionStateMachine (verified during TASK-054). WORK (Core-side, fast-testable): track last-activity tick per claimed seat (any edge or stick movement; likely InputInterpreter or FSM-side using its outputs — design call), have SessionStateMachine derive an Idle status after the GDD threshold (VERIFY the exact number and semantics against the GDD text firsthand — the 45 s figure and 'excluded from payout, not eliminated' semantics come from the TASK-054 audit report and must be re-confirmed against the source; [ASSUMED]-tag any gap the GDD leaves, e.g. whether Idle seats still stake in FINAL_WAGER, whether resume takes effect same-round or next-round, HUD signaling). Integrate with payout application (Idle seat excluded from that round's payout) without breaking pot conservation or the TASK-051/052 pinned tests.",
  "acceptance_criteria": [
    "Per-seat activity tracking marks a claimed seat Idle after the GDD-specified no-input window (threshold verified against the GDD firsthand and cited; [ASSUMED]-tagged if the GDD is ambiguous).",
    "An Idle seat is excluded from the round payout in which it is Idle but remains in the session and resumes on input, per GDD semantics; interactions with FINAL_WAGER/climax are decided and [ASSUMED]-tagged where the GDD is silent.",
    "Tests (tdd tier) pin: idle-threshold crossing, payout exclusion for the idle round, resume behavior, and no regression to pot conservation or existing TASK-051/052 scoring pins.",
    "Fast suite green; any Framework/HUD signaling surface is scoped out or Unity-gated separately."
  ],
  "status": "todo",
  "priority": "medium",
  "labels": [
    "gdd-v2",
    "core",
    "input",
    "scoring",
    "follow-up"
  ],
  "assignee": null,
  "depends_on": [
    "TASK-024",
    "TASK-051"
  ],
  "linked_commits": [],
  "linked_prs": [],
  "comments": [
    {
      "author": "orchestrator",
      "at": "2026-07-07T14:59:41.406Z",
      "body": "RIDERS from TASK-052 review (rev-t052 LOW-2 + LOW-4, doc/cosmetic, 2026-07-07 — this is the next ticket expected to touch SessionStateMachine): (1) LOW-2: fix the stale cross-reference in the Coins property doc (SessionStateMachine.cs:352) — it still says \"See class doc for the [ASSUMED] default-payout note\" but TASK-052 rewrote that paragraph to fallback-only wording; one line. (2) LOW-4 (optional, do together if touching the guard): the coop-table shape guard at ~:654 throws ArgumentException with nameof(_activePayoutTable) — a private field, not an argument on the current stack; InvalidOperationException is semantically accurate; the TASK-052 test pins ArgumentException, so change test + throw together or skip."
    },
    {
      "author": "orchestrator",
      "at": "2026-07-07T15:18:07.962Z",
      "body": "IMPLEMENTER NUANCE (from rev-t054b's TASK-054 audit, 2026-07-07): a HumanIdle value already exists on the SeatState enum (Core/.../Microgames/V2/SeatState.cs:12) and is referenced in PlayerRoster.cs:21 — but NOTHING anywhere assigns or transitions a seat into it. So the target state exists; this ticket supplies the missing detection+transition logic (per-seat last-activity tracking -> mark HumanIdle after the GDD threshold -> exclude from that round's payout). Reuse the existing enum value rather than inventing a new status; grep for existing HumanIdle references first to see what already branches on it (if anything). This also confirms the 'dead-seat detection missing' premise firsthand."
    }
  ],
  "created_at": "2026-07-07T14:53:30.632Z",
  "updated_at": "2026-07-07T15:18:07.962Z",
  "jira_key": null,
  "verification_tier": "tdd",
  "marker": "[EXPLICIT]",
  "source_tier": "T1"
}
