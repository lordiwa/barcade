{
  "key": "TASK-060",
  "title": "T-107 Slice 3 — MECH_01 ¡MANTÉN! new inverted-pendulum balance mechanic",
  "description": "Slice 3 of the TASK-034 (GDD T-107) split — new mechanic. Inverted-pendulum balance per GDD §4 MECH_01, on the canonical §10.2 v2 IMicrogame contract (engine-free Core sim, injected SeededRandom, zero-alloc Tick). AlcocheckSim.cs is prior art to mine, not a constraint. FIDELITY RULED (human 2026-07-07, both [ASSUMED]-tagged + reviewer-verified for determinism and feel): (a) perturbation uses a DETERMINISTIC seeded-noise substitute (value-noise / interpolated, not required to be true Perlin) preserving the ramping amplitude A(t) = A0 + ramp·t — a new engine-free noise helper is in-scope since none exists in Core; (b) keep a small DAMPING term for Euler integration stability, as a documented deviation from the GDD's literal undamped ODE. Model: θ'' = (g/L)·sin(θ) + perturbation(t) − k·input (+ damping); fail at |θ| > θmax (35° default); L/R torque only; victory = last standing or lowest mean |θ|. Declare difficultyScaling; pass the TASK-030 validator.",
  "acceptance_criteria": [
    "MECH_01 implements the v2 IMicrogame contract as an engine-free Core sim (injected SeededRandom, zero-alloc Tick); L/R torque only; fail at |theta| > 35° (default, configurable).",
    "Perturbation is a deterministic seeded-noise substitute with ramping amplitude A(t)=A0+ramp·t; the noise helper and the retained damping term are both [ASSUMED]-tagged as deviations from the GDD-literal ODE.",
    "With null input, every pendulum falls before duration on all test seeds; with simulated perfect correction, 100% survive.",
    "A replay determinism test passes (two independently constructed sims with the same seed produce identical θ trajectories and outcome).",
    "The v2 definition declares difficultyScaling params and passes the TASK-030 validator.",
    "Tests-first (red captured); victory resolution (last-standing or lowest mean |theta|) handles ties without fabricated winners; fast suite green, no regression; legacy consumers keep compiling."
  ],
  "status": "done",
  "priority": "high",
  "labels": [
    "gdd-v2",
    "hito-2",
    "core",
    "microgame",
    "MECH_01",
    "T-107",
    "slice"
  ],
  "assignee": null,
  "depends_on": [
    "TASK-030"
  ],
  "linked_commits": [
    "24d8938",
    "edf1526"
  ],
  "linked_prs": [],
  "comments": [
    {
      "author": "dev-t101",
      "at": "2026-07-07T17:07:57.587Z",
      "body": "Hand-off. tdd two-commit: 24d8938 test RED (whole-project compile-red: CS0234 MantenMicrogame absent, CS0246 MantenParams/SeededValueNoise1D absent) -> edf1526 feat GREEN. Suite 610/610 (578+32: 24 MantenMicrogameV2Tests + 8 SeededValueNoise1DTests). Legacy consumers compile (additive MicrogameId.Manten=3).\n\nNOISE HELPER (Barcade.Core.SeededValueNoise1D, RNG folder): lattice of independently-seeded [-1,1] values drawn ONCE from the injected SeededRandom at construction, smoothstep-interpolated between neighbors; Sample() zero-alloc. NoisePointsPerSecond=4 (private engineering constant, not a GDD param — same status as AlcocheckSim's drunkChangeInterval) makes the wobble continuous vs a discrete re-roll.\n\n[ASSUMED] TAGS (both cite the human ruling, both in-code): (1) value-noise not true Perlin (SeededValueNoise1D + MantenMicrogame class docs); (2) damping term (MantenParams.Damping) for Euler stability, GDD ODE is literally undamped.\n\nTWO MORE ASSUMPTIONS NOT PRE-RULED (flagged, not guessed silently): (3) SHARED perturbation curve across all 4 seats — one noise instance/round, same perturbación(t) applied to every seat's angularAccel each tick, NOT 4 independent per-seat curves. GDD's formula names a single 'seed, t' with no player index, so read as one shared 'gust'. Consequence: seats with identical initial conditions + identical input fall on the EXACT same tick, byte-identical trajectories. Independent per-seat sway is a straightforward change (4 noise instances) if wanted. THIS IS THE ONE REAL DESIGN FORK wanting a human read. (4) difficultyScaling target — GDD names no difficultyScaling list for MECH_01 like §11.1's MECH_04 example; mapped difficultyMult -> perturbAmplitude0+perturbRamp (the 'how strong/dense is the disturbance' analog of Esquiva's hazardSpeed+spawnRate), leaving gravityFactor/torqueGain/thetaMax as fixed physics constants. Declared DifficultyScaling=[perturbAmplitude0,perturbRamp].\n\nCORRECTION ORACLE (CorrectionOracle, test-only): reactive PD-style full-authority bang-bang, NOT a hardcoded survivor. Reads ONLY RenderEntity.Rotation (theta degrees — the only channel a real observer of rendered tilt has) + its own previous-tick memory for a finite-difference velocity estimate; never touches sim private state. errorSignal = theta + Kd*velocityEstimate (Kd=0.15), pushes full E/W on deadband cross, SAME sign as predicted lean (the GDD formula subtracts torqueGain*input, so countering disturbance D needs input of the SAME sign as D — derived + documented in the class doc). Diagonal-input test proves the sign/magnitude convention; the 20-seed oracle sweep (real PCG32) proves it holds under noise.\n\nGddDefaults (tuned via a THROWAWAY System.Random prototype in scratch — NOT committed; committed tests use real PCG32 SeededRandom): gravityFactor=3.0, perturbAmplitude0=1.2, perturbRamp=0.3, torqueGain=10.0, thetaMaxDegrees=35 (GDD literal), damping=1.2, durationSeconds=8.0 (top of GDD's 5-8s note — the extra time is what makes null-input-always-falls reliable across seeds). Both AC3 sweeps (null-falls seeds 0-19, oracle-survives seeds 0-19) passed FIRST run vs real PCG32, no iteration.\n\nZERO-ALLOC SENSOR (M1, written in-ticket per the brief): SteadyStateTick_AllocatesNoManagedMemory uses gravityFactor=perturbAmplitude0=perturbRamp=0 (every force term EXACTLY zero, not a probabilistic margin) so liveness across the 1000-tick window is mathematically guaranteed, while the full noise+integration hot path still executes (Sample IS called, result multiplied by amplitude=0, not skipped). MUTATION-RED: new int[1] as Sample's first line -> 'Failed SteadyStateTick_AllocatesNoManagedMemory / Expected: 0 / But was: 32000', reverted, grepped clean.\n\nGDD AMBIGUITY SURFACED (item 3 above) is the one I'd most want the human's read on — a real design fork, picked the literal-formula-favoring interpretation without a ruling behind it."
    },
    {
      "author": "rev-t060",
      "at": "2026-07-07T17:30:17.862Z",
      "body": "FULL review verdict (24d8938 + edf1526, pinned worktree at edf1526): APPROVE-WITH-NITS — 0 HIGH, 0 MEDIUM, 1 non-blocking LOW + 2 observations. All 6 ACs met; all 3 load-bearing points verified firsthand. Suite 610/610 firsthand (578+32 counted directly); RED at 24d8938 reproduced (CS0234/CS0246 on the absent types); mutation-red reproduced (new int[1] in Sample -> 'Expected: 0 But was: 32000' = 1 alloc/tick x 1000, ONLY failure). Both worktrees removed.\n\n(3) CORRECTION-ORACLE VALID: CorrectionOracle.Decide reads only rs.Entities[i].Rotation for the matching OwnerSeat + its own _prevTheta finite-difference velocity, never sim private state. Sign convention CORRECT against the integration code: sim does '... − TorqueGain·horizontalInput' (MantenMicrogame.cs:283-287), ToUnitVector(E)=(+1,0), so positive lean -> oracle emits E (input +1) -> −TorqueGain·(+1)<0 -> restores. Both AC3 sweeps genuine/non-vacuous (20 seeds null->all fall; 20 seeds oracle->all survive; ~1.9x torque margin makes survival physical). OBSERVATION: oracle is idealized/superhuman (60Hz exact angle) — exactly what AC2's 'corrección perfecta simulada' asks, correct here, but does NOT establish HUMAN winnability at GddDefaults -> playtest flag, not this ticket.\n\n(4) ZERO-ALLOC SENSOR NON-VACUOUS: Sample-runs-with-amplitude-0 PASSES — Tick calls _noise.Sample(_elapsedSeconds) UNCONDITIONALLY at :273 before the amplitude(=0) multiply, no short-circuit; mutation-red is direct proof Sample runs every measured tick. Liveness bracket real (all forces zeroed -> θ never moves -> round never finishes; IsFinished==False asserted before line 433 AND after line 439; 30s round vs 1000-tick window). Isolated (609 green under mutation).\n\n(5) SHARED-GUST MATCHES DISCLOSURE: exactly ONE SeededValueNoise1D per round (:246), ONE perturbation value/tick (:273-274) added identically to every active seat (:285). Not per-seat.\n\nLOW-1 (test-adequacy, non-blocking): the shared-gust byte-identical consequence (identical seats fall same tick) is correct by construction but NOT pinned by a discriminating test — neither NullInput_EveryPendulumFalls nor the determinism test (per-seat different inputs) would fail if changed to per-seat noise. Since shared-gust is itself pending human ratification, locking it now may be premature; if ratified, a one-line same-fall-tick assertion under GddDefaults closes it. NIT: oracle superhuman -> human-winnability unproven (playtest). NIT: null-input AC uses 20 seeds, reliability rests on empirical tuning (0/5000 scratch) — adequate as a lock.\n\nSTANDARD ALL PASS: RNG purity proven by the EXECUTABLE GDD §13 guard (CoreSources_NeverReferenceSystemRandomOrUnityRandom, Pcg32SeededRandomTests.cs:157, recursively scans Core/Runtime *.cs comments-stripped) green — corroborates scratch-only prototype. Noise correctness (smoothstep C1-continuous, [-1,1), zero-alloc Sample; 8 tests non-vacuous). Determinism AC4 non-trivial (real ramping noise, same-seed identical, different-seed diverges). AC5 schema gate PROVEN (torqueGain=999 out-of-[1,20] with every other field valid isolates the range check). Victory/ties AC6 identical Ranked/PlayerRank shape to Esquiva, SessionStateMachine-compatible, GDD rule verbatim (4 ranking tests). All [ASSUMED] tags cite the ruling (value-noise, damping, shared-gust, difficultyScaling mapping); thetaMax 35° strict > matches. GDD §MECH_01 (lines 247-265) citations verified. Zero-alloc Tick holds beyond the sensor. Tier tdd honored."
    },
    {
      "author": "orchestrator",
      "at": "2026-07-07T17:30:38.460Z",
      "body": "CLOSED on rev-t060 APPROVE-WITH-NITS (0 HIGH / 0 MEDIUM / 1 non-blocking LOW + 2 observations). Review depth FULL; rubric: core tdd microgame logic + a NEW engine-free noise primitive (mandatory-FULL), 1475-line diff. Third T-107 mechanic slice done (Slice 3 of the TASK-034 umbrella). Delivered: MECH_01 ¡MANTÉN! inverted-pendulum on the v2 IMicrogame contract (engine-free, injected SeededRandom, zero-alloc Tick, L/R torque, fail |θ|>35°, last-standing/lowest-mean-|θ| victory) + the new SeededValueNoise1D primitive (seeded smoothstep lattice, zero-alloc Sample). ALL THREE LOAD-BEARING POINTS VERIFIED FIRSTHAND BY REVIEWER: (3) correction-oracle VALID (observable-channels-only, sign convention checked against the integration code = restores not backwards, both AC3 sweeps physical); (4) zero-alloc sensor NON-VACUOUS (Sample called unconditionally before the amplitude-0 multiply, mutation-red 32000 reproduced, liveness bracket real); (5) shared-gust implementation matches disclosure (one noise instance, one value/tick to all seats). RNG purity proven by the executable GDD §13 guard. Commits 24d8938 (red, CS0234/CS0246 reproduced) / edf1526 (feat); 578->610 reviewer-reproduced. Tier tdd honored. LOW-1 (shared-gust byte-identical consequence unpinned) DEFERRED pending the shared-gust human ratification: if ratified -> add a one-line same-fall-tick lock; if ruled per-seat -> a real change (4 noise instances). [ASSUMED] ratification list additions: shared-gust perturbation (+ its conditional lock rider), difficultyScaling mapping (perturbAmplitude0+perturbRamp); value-noise + damping already ruled. Observation for playtest: the oracle is superhuman, so human-winnability at GddDefaults is unproven (UAT concern, not code)."
    }
  ],
  "created_at": "2026-07-07T15:12:07.239Z",
  "updated_at": "2026-07-07T17:30:38.460Z",
  "jira_key": null,
  "verification_tier": "tdd",
  "marker": "[EXPLICIT]",
  "source_tier": "T1"
}
