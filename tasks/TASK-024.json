{
  "key": "TASK-024",
  "title": "GDD T-102 — SessionStateMachine: full session FSM with per-state timeouts (§2.1/§2.2)",
  "description": "Extend/wrap the existing RoundPhaseMachine (Barcade.Core — do NOT rewrite it; add states, per Annex D.1) into a SessionStateMachine covering the full GDD §2.1 graph: ATTRACT -> JOIN -> (BOARD_MOVE -> BOARD_RESOLVE -> MG_INTRO -> MG_PLAY -> MG_RESULT -> INTERMISSION)* -> FINAL_WAGER -> FINAL_MG -> GAME_OVER -> ATTRACT. Board states are part of the FSM now but pass through as stubs until BoardModel (Hito 4) is wired. FSM invariants from the GDD: no transition ever blocks on input (every state has a timeout, §2.2 budget table); MG_PLAY is the only state where gameplay input reaches the active microgame; state serializes per tick for deterministic replay (§13). JOIN: each player claims a color with a button press, needs >= 2 ready players, 30 s timeout; GAME_OVER returns to ATTRACT after 20 s. Read GDD §2 before coding.",
  "acceptance_criteria": [
    "SessionStateMachine implements every state and transition of GDD 2.1 including JOIN color-claim (>=2 players, 30 s timeout), the round loop, FINAL_WAGER, FINAL_MG, and GAME_OVER (20 s timeout back to ATTRACT)",
    "Every state has a timeout from the GDD 2.2 budget table and a session driven with zero input still reaches GAME_OVER (no state can be blocked by an absent player)",
    "Only MG_PLAY forwards gameplay input to the active microgame; all other states accept only confirmation taps / minimal navigation",
    "BOARD_MOVE and BOARD_RESOLVE exist as pass-through stubs (advance in <= 1 tick when no board model is wired) so Hito 4 slots in without changing the FSM shape",
    "FSM state is a serializable POCO and the machine is deterministic: same (seed, input sequence) produces an identical state trace (replay test)",
    "Existing RoundPhaseMachine consumers keep compiling and passing (extension, not rewrite)",
    "Tests-first: failing fast-suite tests for the invariants above committed strictly before implementation; suite green after with no regression"
  ],
  "status": "in_review",
  "priority": "high",
  "labels": [
    "gdd-v2",
    "hito-1",
    "core",
    "fsm",
    "T-102"
  ],
  "assignee": "developer-cloud",
  "depends_on": [
    "TASK-023"
  ],
  "linked_commits": [
    "03a8d77",
    "13d45bf"
  ],
  "linked_prs": [],
  "comments": [
    {
      "author": "developer-cloud",
      "at": "2026-07-07T09:05:00.000Z",
      "body": "[developer-cloud] TASK-024 delivered on branch claude/gdd-hito1-cloud, tests-first. 03a8d77 test(core): failing spec, red = CS0246 compile errors on SessionStateMachine/SessionState/SessionConfig/SessionStateSnapshot (22 tests encoding all 7 ACs). 13d45bf feat(core): implementation. Suite 449 -> 471 green, zero regressions. Design notes for reviewer: (1) wraps RoundPhaseMachine per Annex D.1 — MgIntro/MgPlay ride its CommandShow/Play phases; rpm constructed with zero-width Intermission because the GDD 2.1 graph moved the intermission to end-of-round (session-timed); two bounded divergence windows documented in the class doc (early finish, overtime), rpm re-armed every MG_INTRO. (2) [ASSUMED] FinalWagerSeconds=10 — GDD 2.2 budget table has no FINAL_WAGER row but the 2.1 invariant requires every state to time out; flagged for T-109/pilot recalibration. (3) [ASSUMED] JOIN starts early when all 4 seats claim (nobody left to wait for); GDD only specifies >=2 + 30 s. (4) JOIN start-check runs before the tick's claims, so a 4th-claim start lands next tick (deterministic). (5) Gameplay input forwarded ONLY in MgPlay/FinalMg with exit-before-forward ordering — the transition tick never forwards; FINAL_MG treated as a play state (the climax microgame must read input; GDD 2.1 invariant read as gating non-play states). (6) Board stubs consume exactly 1 tick each. (7) Machine never calls IMicrogame.Initialize — host stages already-initialized microgames via StageRound during board-stub/FINAL_WAGER windows; unstaged rounds time out on DefaultMgPlaySeconds=5 (liveness over content). (8) sessionSeed stored/exposed, not yet consumed (FSM has no randomness until BoardModel); replay determinism test covers (seed, credit tick, input sequence) -> identical Capture() traces. (9) Zero-alloc steady-state proven via GC.GetAllocatedBytesForCurrentThread."
    }
  ],
  "created_at": "2026-07-07T03:25:56.079Z",
  "updated_at": "2026-07-07T09:05:00.000Z",
  "jira_key": null,
  "verification_tier": "tdd",
  "marker": "[EXPLICIT]",
  "source_tier": "T1"
}