{
  "key": "TASK-026",
  "title": "GDD T-104 — MECH_04 ¡APUNTA! hold-charge aiming microgame (§4 MECH_04, IMicrogame contract §10.2)",
  "description": "Highest-priority mechanic. Pure Barcade.Core sim implementing IMicrogame (§10.2). Rules per §4 MECH_04: each player owns a corner turret in normalized [0,1]² space; the stick sets one of 8 discrete angles (the 0.25 s interpolation is presentation — sim tracks the discrete angle plus an interp factor exposed via RenderState); holding the button charges with the oscillating meter p(t) = 0.5*(1 + sin(w*t_hold)) where the full cycle lasts chargeCycleSec (1.2 s default); release fires a ballistic projectile (angle, p); targets appear at seeded positions in the central zone. Scoring: most hits in duration; tiebreak by summed precision (distance to target center). Edge cases from the GDD: button still held at timeout auto-fires with current power (nobody loses their shot); two projectiles hitting the same target the same tick — higher precision scores, the other passes to the next target if one exists. Params: chargeCycleSec, targetCount, targetMoving (enabled+speed), windAccel, projectileSpeedMin/Max, duration. Read GDD §4 MECH_04, §10.2, Annex D.2. Uses the TASK-023 InputInterpreter for hold tracking.",
  "acceptance_criteria": [
    "Implements the IMicrogame contract as engine-free Core code; all randomness through injected SeededRandom",
    "Charge power follows p(t)=0.5*(1+sin(w*t_hold)) with full cycle = chargeCycleSec; a hold of duration d starting at the same tick always yields the same power (determinism test)",
    "Release fires a ballistic projectile parameterized by (discrete angle, power); trajectories are deterministic in logical [0,1]² space; windAccel variant shifts trajectories deterministically",
    "Targets spawn at seeded central-zone positions honoring targetCount and targetMoving params; every target is reachable from all 4 corners for every test seed (solver test, GDD AC2)",
    "Button still held when duration expires auto-fires with the instantaneous power (timeout test, GDD AC3)",
    "Two projectiles on the same target in the same tick: the more precise one scores, the other passes to the next remaining target if any (edge-case test)",
    "Result ranks by hit count with summed-precision tiebreak; same-tick equal outcomes share Place",
    "Steady-state Tick allocates zero heap memory (projectiles pooled)",
    "Tests-first: red tests for each AC committed strictly before implementation; fast suite green after, no regression"
  ],
  "status": "in_review",
  "priority": "critical",
  "labels": [
    "gdd-v2",
    "hito-1",
    "core",
    "microgame",
    "MECH_04",
    "T-104"
  ],
  "assignee": null,
  "depends_on": [
    "TASK-023"
  ],
  "linked_commits": [],
  "linked_prs": [],
  "comments": [
    {
      "author": "developer",
      "at": "2026-07-07T05:05:18.518Z",
      "body": "Implemented tests-first on master by dev-t101, no push, no Unity. Commits: a6337ed test(core) failing ApuntaMicrogame v2 spec (16 tests; red = 2x CS0246 on the not-yet-existing ApuntaParams, captured before ANY production file existed) -> c7c0322 test(core) two pre-implementation test-setup fixes (same-tick-contest test was tie-by-construction via mirror-symmetric shots; two tests held button only 1 tick which T-101's 2-tick debounce never confirms) -> 847d43b feat(core) ApuntaMicrogame v2. Suite 378 -> 394 green. Files: V2/ApuntaParams.cs, V2/ApuntaMicrogame.cs, MicrogameId.cs (+Apunta), Tests/EditMode/ApuntaMicrogameV2Tests.cs (+metas, 3 hand-minted collision-checked). Contract: current GDD-literal v2 (Initialize(SeededRandom,PlayerRoster,float), Tick(in InputSnapshot)); flagged that the original briefing's ISeededRandom mention was stale vs a7c59ab — followed master's literal contract. DESIGN ASSUMPTIONS (GDD silent, flagged for reviewer): (1) HitRadius 0.18, central zone [0.4,0.6]^2, projectile speed 0.45-0.95 — solver-verified reachable across 150 seeds x 3 targets x 4 corners via brute-force numerical solver (8 directions x 400 power samples using the sim's own landing math); (2) fixed ProjectileFlightSeconds 0.3 independent of power/distance; (3) same-tick contest = greedy nearest-pair matching, cascades to further targets; (4) ranking = hits desc then summed precision asc, competition ranking (1,1,3). Aiming rules (GDD gaps, pinned by tests): aim reads live 8-way RawDirection; stick None holds last non-None aim; cold-start default = corner-to-center diagonal so no turret faces out of arena. Risks/notes: InputBridge ~15 lines DUPLICATED from ReaccionaMicrogame (deliberate — Reacciona review was in flight; follow-up cleanup ticket candidate); WindAccel shifts landing X only (documented choice); PoolCapacity 32 / FeedbackCapacity 16 with silent-drop defensive guards (mash-ring precedent); difficultyMult accepted but no-op (GDD defines no MECH_04 scaling); two ApuntaParams validation checks relaxed > to >= to permit degenerate param spaces (single-point zone, fixed speed)."
    },
    {
      "author": "reviewer",
      "at": "2026-07-07T05:30:19.622Z",
      "body": "FULL-depth fresh-context review (rev-t026) of a7c59ab..847d43b, evidence pinned at 847d43b in a detached worktree: REQUEST-CHANGES — 1 HIGH, 3 MEDIUM, 5 LOW. Suite 394/394 observed pinned. Commit order + compile-red verified from tree state; c7c0322 judged LEGITIMATE pre-impl test-authoring corrections (old contest setup tied by mirror symmetry — even a wrong impl passed; 1-tick holds were unpassable against the pre-existing 2-tick debounce; both fixes strengthen the sensors). HIGH-1 (ApuntaMicrogame.cs:244-283): firing is not gated on round duration — the overtime window kept open for in-flight shots still runs the per-seat charge/fire loop; (A) hold through expiry -> auto-fire at durationTicks-1, then releasing within ~0.25 s fires a SECOND projectile from the same physical hold which can score and change ranking (violates GDD §4 'más impactos en duration' and the code's own last-active-tick comment); (B) liveness: each overtime fire extends AnyProjectileActive ~18 ticks vs ~4-tick min fire cycle -> mashing keeps IsFinished false indefinitely, future sequencer hangs. Fix shape: gate charge accumulation + fire on _tick < durationTicks; overtime only MoveTargets/ResolveArrivals/PublishRenderState. Timeout test never releases so it cannot see this. MEDIUM-1: vacuous zero-alloc window, THIRD occurrence of the class — 982/1000 measured ticks are post-IsFinished no-ops; Fire path never measured, CueHit never executes anywhere in the test (both shots geometrically miss). MEDIUM-2: render contract gap — in-flight projectiles never published (EntityKind.Projectile exists but Entities capacity SeatCount+TargetCount cannot hold them); no charge-meter value exposed (Annex D.2 HudState 'medidores'; the meter is THE visual the player times); GDD §4's '8 direcciones interpoladas a 0.25 s' clause silently dropped — the ticket description explicitly requires an interp factor in RenderState. MEDIUM-3: targetMoving entirely untested (MoveTargets + velocity spawn zero coverage) AND latent defect: single-reflection bounce diverges when zone span < per-tick movement (amplitude grows unboundedly; reachable with valid ctor args via the relaxed >= validation, not with GddDefaults). LOWs: (1) AC5 power-at-timeout never asserted; (2) AC6 positive cascade branch untested (algorithm traced correct); (3) InputBridge copies ALREADY diverge textually (normalized diagonals vs ±1 vectors; identical only while ToDirection8 is sign-only) -> consolidation ticket; (4) difficultyMult no-op comment claims 'GDD defines no MECH_04 scaling' — factually wrong, GDD 11.1's example IS MECH_04 and declares difficultyScaling [targetMoving.speed, windAccel]; deferral to T-106 is defensible, stating it as a GDD fact is calibration laundering — fix comment; (5) flight-vs-pool invariant unguarded (flight >= ~0.54 s at max cadence makes silent shot-drop reachable; guard ctor or document). Positive verdicts: charge math GDD-literal w=2pi/cycle (no factor-of-2 error); solver test genuine (quantization margin analysis: max landing gap ~0.022 << 0.18 hitRadius); 'nobody loses their shot' honored (overtime resolution works — the gate must not remove it); ranking parity with Reacciona confirmed; MicrogameId append-only; metas clean. Pre-existing repo-wide duplicate .meta GUID (8404be70..., TASK-006 era, ArcadeControls vs InputSystem_Actions) -> separate hygiene ticket. Tier-audit: tdd correct and honored. Disposition: fix round HIGH-1 + M1/M2/M3 + LOWs 1/2/4/5 (LOW-3 excluded, separate ticket); rev-t026 re-verifies."
    }
  ],
  "created_at": "2026-07-07T03:26:21.103Z",
  "updated_at": "2026-07-07T05:30:19.622Z",
  "jira_key": null,
  "verification_tier": "tdd",
  "marker": "[EXPLICIT]",
  "source_tier": "T1"
}
